/**
 * Capa de modelo del sistema SGPP.
 *
 * <p>Las clases de este paquete representan las tablas definidas en el PDF de
 * base de datos. Se mantienen como POJOs para que la capa DAO haga el mapeo
 * JDBC sin depender de JavaFX.</p>
 */
package sgpp.modelo;
